Apps					Filter					SSOAuth
							| <-- User
		 <- has session ? - |
doFilter <- Y --|			|
		 -- N -------------	| ------- goto ----> has session ?
doFilter <- add session ---	| -- goto -- Y -----
							|		 |-- N --> Login <---|
							|					|----N---|
doFilter <- add session ---	| -------- Y -------|
							|